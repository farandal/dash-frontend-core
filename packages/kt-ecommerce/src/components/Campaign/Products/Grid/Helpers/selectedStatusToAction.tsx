const selectedStatusToAction = (selectedStatus): string => {

    // switch(selectedStatus) {
    //     case "UNPUBLISH":
    //         return "finish"
    //     case "PAUSE":
    //         return "pause"
    //     case "PUBLISH":
    //     default:
    //         return "publish"
    // }
    return selectedStatus
}

export default selectedStatusToAction;