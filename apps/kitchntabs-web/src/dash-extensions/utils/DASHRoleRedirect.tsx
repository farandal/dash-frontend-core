import { FC } from "react";

// Deprecated, do not use.
const CalculateRedirect: FC = () => {

   /* const [path, setPath] = useState<string | null>(null);

    useEffect(() => {
    
            const path = DASHAuthenticationService.getPendingRedirect();
              
            if (path) {
                setPath(DASHAuthenticationService.getPendingRedirect());
            }

      
    }, []);

    return path ? <Redirect path={path} /> : null
    */

    return <></>


};

export default CalculateRedirect;