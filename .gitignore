**/node_modules
**/dist
**/build
**/_dash
**/.env*
!**/.env.template
.turbo
yarn-error.log
apps/dash
yarn.lock
**/.DS_Store
.gitmodules
release
icons
apps/dash
build-resources
*.mp3
*.wav
android/app/src/main/res/*
build_config.json
apps/minimal
apps/v2

# Temporary and backup files
apps_bkup/
bkup/
typecheck_output*.txt

# Electron runtime files
**/dash_process.lock
**/logs/
**/output/

# build-electron.js temporarily renames these mid-build to work around an
# electron-builder pnpm-workspace parsing bug; they must never be committed -
# doing so once already leaked a 23k-line stale pnpm-lock.yaml into git history.
*.build-backup
package.json.backup
